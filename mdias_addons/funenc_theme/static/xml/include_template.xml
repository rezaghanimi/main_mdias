<?xml version="1.0" encoding="UTF-8"?>
<templates id="theme_include_template" xml:space="preserve">
    <t t-name="ThemeNotification">
        <div t-attf-class="o_notification #{widget.className}" t-translation="off" role="alertdialog">
            <a t-if="widget.sticky" class="fa fa-times o_close" href="#" title="Close" aria-label="Close"/>
            <div class="o_notification_title">
                <t t-if="widget.type === 'warning'">
                    <t t-set="icon_class" t-value="'el-notification__icon el-icon-warning'"/>
                </t>
                <t t-else="">
                    <t t-set="icon_class" t-value="'el-notification__icon el-icon-info'"/>
                </t>

                <i t-attf-class="o_icon  #{icon_class} #{widget.icon}" role="img" t-attf-aria-label="Notification #{widget.name}" t-attf-title="Notification #{widget.name}"/>
                <t t-raw="widget.title"/>
            </div>
            <div class="o_notification_content" t-if="widget.message">
                <t t-raw="widget.message"/>
            </div>
            <div t-if="widget.buttons.length" class="o_buttons">
                <button t-foreach="widget.buttons" t-as="button" t-attf-class="btn #{button.class_names || (button.primary ? 'btn-primary' : 'btn-secondary')}" type="button">
                    <t t-if="button.icon">
                        <i t-if="button.icon.indexOf('fa-') === 0" t-attf-class="fa fa-fw o_button_icon #{button.icon}" role="img" t-att-aria-label="button.name" t-att-title="button.name"/>
                        <img t-else="" t-att-src="button.icon" t-att-alt="button.name"/>
                    </t>
                    <span><t t-esc="button.text"/></span>
                </button>
            </div>
        </div>
    </t>


</templates>